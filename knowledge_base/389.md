## LDAP (Port 389)
* [ ] **Null Bind:** `ldapsearch -x -h <IP> -s base namingContexts`
* [ ] **Dump Users:** `ldapsearch -x -h <IP> -b "DC=example,DC=com" "(objectClass=person)"`
* [ ] **AD Info:** Use `windapsearch` for deeper AD object mapping.